// This is the Researcher's view
import ResearcherSearch from '@/components/ResearcherSearch';

export default function CollaboratorsPage() {
  // We re-use the same component with the other title!
  return <ResearcherSearch title="Find Collaborators" />;
}